import styled from 'styled-components'

export const NavbarItem = styled.span`
   max-width: ${props => props.theme.breakpoints.desktopBk};
   position: relative;
   margin: 0 auto;
   flex-grow: 1;
`